// Indexer role — placeholder for Phase A.
//
// Phase A keeps compaction inside the worker process via
// `pe_idx::run`. The standalone IDX runner with job-pull semantics
// lands in Phase D alongside the JobQueue gRPC service.
