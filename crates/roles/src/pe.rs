// Plan Executor role — placeholder for Phase A.
//
// Phase A keeps the composite `pe_idx::run` in charge. The
// standalone PE runner lands in Phase D when compaction / index
// build move to the IDX role and PE becomes pure read/write
// execution with an NVMe cache.
